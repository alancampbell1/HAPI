Please note you must install the following to get the code working with NodeJS:

Install express:
npm install express --save

Install body-parser:
npm install body-parser

Install handlebars:
npm install express-handlebars

Install nodemailer:
npm install nodemailer --save
